<div class="container">
  <h3>Wish List:</h3>

  {{#each wishlistItemData as |wishlist|}}
    <div class="row">
      <div class="col s9">
        <h4><a href="/videos/{{wishlist.wishlistItem.video_id}}">Test</a></h4>
      </div>
      <div class="col s2">
        <button
          class="delete-video waves-effect waves-light btn"
          data-videoId="{{wishlist.wishlistItem.video_id}}"
        >DELETE</button>
      </div>
    </div>
  {{/each}}
</div>

<script src="./js/wishlist.js"></script>